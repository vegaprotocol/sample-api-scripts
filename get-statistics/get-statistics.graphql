query getStatistics {
    statistics {
        chainVersion
        appVersion
        status
        totalTrades
        totalOrders
        totalPeers
        totalMarkets
        ordersPerSecond
        tradesPerSecond
        averageOrdersPerBlock
        averageTxBytes
    }
}